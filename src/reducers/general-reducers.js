// import {
//     ACTION_NAME,
//     ACTION_NAME
// } from '../actions/component-action';

const initialState = {
    // name: value,
    // name: value
};

export default function reducer(state = initialState, action) {
    // if (action.type === ACTION_NAME) {
    //     return Object.assign({}, state, {
    //         name: value,
    //         name: value
    //     });
    // } else if (action.type === ACTION_NAME) {
    //     return Object.assign({}, state, {
    //         name: value
    //     });
    // }
    return state;
}
